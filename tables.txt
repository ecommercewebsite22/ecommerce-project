CREATE TABLE user_info(
  user_id SERIAL PRIMARY KEY AUTO,
  username varchar(50),
  password varchar(50),
  first_name varchar(20),
  last_name varchar(20),
  email varchar(50),
  user_type varchar(10)
);

CREATE TABLE products(
  product_sku SERIAL PRIMARY KEY AUTO,
  product_name varchar(30),
  product_image varchar(255),
  product_quantity INT,
  product_price NUMERIC
);

CREATE TABLE cart(
  cart_no SERIAL PRIMARY KEY AUTO,
  user_id INT REFERENCES user_info(user_id),
  product_sku INT REFERENCES products(product_sku),
  
  

CREATE TABLE order_history(
  order_no SERIAL PRIMARY KEY AUTO,
  account_id INT REFERENCES customer_profile(account_id),
  sku INT,
  product_name varchar(30),
  unit_cost NUMERIC,
  quantity INT,
  date TIMESTAMP
);
